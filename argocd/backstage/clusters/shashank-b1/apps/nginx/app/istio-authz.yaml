apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: nginx
  namespace: nginx
  labels:
    backstage.io/kubernetes-id: nginx
spec:
  action: ALLOW
  rules:
  - from:
    - source:
        namespaces:
          - curl